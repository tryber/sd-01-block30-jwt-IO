

describe('2 - Crie um endpoint para realizar login na API', () => {
  it('O endpoint deve receber requisições POST na rota /login.', () => {
    
  });
  it(`O endpoint deve receber um nome de usuário e senha, no formato abaixo:
      username: johndoe,
      password: senhasupersecreta`, () => {
    
  });
  it(`O endpoint deve consultar se existe um usuário com as credenciais informadas.
      Caso exista, deve ser gerado um token JWT como resposta.`, () => {
    
  });
  it(`O token retornado deve expirar em 3 dias e deverá levar em seu payload as propriedades username e role do usuário.
      O formato da resposta deve ser como exemplificado abaixo:
      token: <token>,
      expires: <tempo>,`, () => {
    
  });
})
